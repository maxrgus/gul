 <div class="middle-box text-center loginscreen animated fadeInDown">
        <div>
            <div>

                <%= image_tag('logotype.png') %>
                <%= form_for(resource, as: resource_name, url: session_path(resource_name), class: 'm-t') do |f| %>
                  <div class="form-group">
                    <%= f.email_field :email, autofocus: true, placeholder: "Email", class: 'form-control' %>
                  </div>
                  <div class="form-group">
                    <%= f.password_field :password, autocomplete: "off", placeholder: "Lösenord", class: "form-control" %>
                  </div>
                    <%= f.submit "Logga in", class: "btn btn-primary block full-width m-b" %>
                <% end %>
        </div>
    </div>


